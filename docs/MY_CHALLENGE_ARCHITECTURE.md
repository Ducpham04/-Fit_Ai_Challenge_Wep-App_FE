# My Challenge - Architecture & Component Diagram

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    User Interface Layer                      │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │         MyChallengePage (Landing Page)              │   │
│  │  ┌────────────────────────────────────────────────┐ │   │
│  │  │ User Header & Stats                            │ │   │
│  │  ├────────────────────────────────────────────────┤ │   │
│  │  │ MyTrainingPlans Component                      │ │   │
│  │  │ ├─ Plan Card 1                                 │ │   │
│  │  │ ├─ Plan Card 2                                 │ │   │
│  │  │ └─ Plan Card 3                                 │ │   │
│  │  └────────────────────────────────────────────────┘ │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │    TrainingPlanDetailPage (Plan Detail View)        │   │
│  │  ┌────────────────────────────────────────────────┐ │   │
│  │  │ TrainingPlanHeader                             │ │   │
│  │  ├────────────────────────────────────────────────┤ │   │
│  │  │ DayTabs (Day Navigation)                       │ │   │
│  │  │ [Day 1] [Day 2] [Day 3] ...                   │ │   │
│  │  ├────────────────────────────────────────────────┤ │   │
│  │  │ ChallengeCard List                             │ │   │
│  │  │ ├─ Challenge 1                                 │ │   │
│  │  │ ├─ Challenge 2                                 │ │   │
│  │  │ └─ Challenge 3                                 │ │   │
│  │  └────────────────────────────────────────────────┘ │   │
│  │                                                     │   │
│  │  ┌────────────────────────────────────────────────┐ │   │
│  │  │ ChallengeDetailModal (Video Upload)            │ │   │
│  │  │ ├─ Challenge Info                              │ │   │
│  │  │ ├─ Video Upload Area                           │ │   │
│  │  │ └─ AI Analysis Results                         │ │   │
│  │  └────────────────────────────────────────────────┘ │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
└─────────────────────────────────────────────────────────────┘
         ↓
┌─────────────────────────────────────────────────────────────┐
│                  Business Logic Layer                        │
├─────────────────────────────────────────────────────────────┤
│  React Hooks & State Management                            │
│  ├─ useState (Plan, Day, Modal state)                      │
│  ├─ useEffect (Data loading)                               │
│  └─ useMemo (Performance optimization)                     │
└─────────────────────────────────────────────────────────────┘
         ↓
┌─────────────────────────────────────────────────────────────┐
│                  API Service Layer                          │
├─────────────────────────────────────────────────────────────┤
│  myChallengeService.ts                                     │
│  ├─ getCurrentTrainingPlans()                              │
│  ├─ getTrainingPlanDetail()                                │
│  ├─ getChallengByDay()                                     │
│  ├─ submitChallengeVideo()                                 │
│  ├─ updateChallengeStatus()                                │
│  └─ [+ future API calls]                                   │
└─────────────────────────────────────────────────────────────┘
         ↓
┌─────────────────────────────────────────────────────────────┐
│              Backend API / Data Layer                       │
├─────────────────────────────────────────────────────────────┤
│  /api/v1/training-plans                                    │
│  /api/v1/training-plans/{id}                               │
│  /api/v1/training-plans/{id}/days/{dayNumber}              │
│  /api/v1/training-plans/{id}/challenges/{challengeId}      │
│  [+ AI Analysis Service]                                   │
└─────────────────────────────────────────────────────────────┘
```

---

## 📊 Component Hierarchy

```
App
│
└── MainLayout
    └── Routes
        └── /my-challenge
            │
            ├── MyChallengePage
            │   ├── Header
            │   ├── StatsOverview
            │   │   ├── ActivePlansCard
            │   │   ├── ChallengesCompletedCard
            │   │   └── StreakCard
            │   │
            │   └── MyTrainingPlans
            │       └── PlanCard[] (selectable)
            │
            └── TrainingPlanDetailPage (when plan selected)
                ├── TrainingPlanHeader
                │   ├── PlanInfo
                │   ├── UserInfo
                │   └── ProgressBar
                │
                ├── DayTabs
                │   └── DayButton[]
                │
                ├── ChallengeCard[]
                │   ├── ChallengeInfo
                │   ├── StatusBadge
                │   ├── AIAnalysisDisplay (if completed)
                │   └── ActionButtons
                │
                └── ChallengeDetailModal (when challenge clicked)
                    ├── ChallengeDetails
                    ├── AIAnalysisResults (if exists)
                    └── VideoUploadForm
                        ├── FileInput
                        ├── UploadProgress
                        └── SubmitButton
```

---

## 🔄 Data Flow Diagram

```
┌─────────────┐
│   User      │
│ Interaction │
└──────┬──────┘
       │
       ↓
┌─────────────────────────┐
│ Component State Update   │
│ - Selected Plan          │
│ - Selected Day           │
│ - Selected Challenge     │
│ - Modal Open/Close       │
└──────┬──────────────────┘
       │
       ↓
┌─────────────────────────┐
│ API Call                │
│ (myChallengeService)    │
└──────┬──────────────────┘
       │
       ├─→ getCurrentTrainingPlans()
       ├─→ getTrainingPlanDetail()
       ├─→ getChallengByDay()
       ├─→ submitChallengeVideo()
       └─→ updateChallengeStatus()
       │
       ↓
┌──────────────────────┐
│ Backend / Mock Data  │
└──────┬───────────────┘
       │
       ↓
┌──────────────────────────┐
│ Response Processing      │
│ - Parse data             │
│ - Update state           │
│ - Handle errors          │
└──────┬───────────────────┘
       │
       ↓
┌──────────────────────────┐
│ Component Re-render      │
│ - Update UI              │
│ - Show results           │
│ - Display feedback       │
└──────┬───────────────────┘
       │
       ↓
┌──────────────────────────┐
│ User Sees Updated View   │
└──────────────────────────┘
```

---

## 🎬 Video Upload Flow

```
User Selection
      │
      ↓
┌──────────────────────┐
│ Click Upload Button  │
└──────┬───────────────┘
       │
       ↓
┌──────────────────────────────────────┐
│ ChallengeDetailModal Opens           │
│ - Display challenge details          │
│ - Show video upload area             │
└──────┬───────────────────────────────┘
       │
       ↓
┌──────────────────────────────────────┐
│ User Selects Video File              │
└──────┬───────────────────────────────┘
       │
       ↓
┌──────────────────────────────────────┐
│ File Validation                      │
│ - Check file type                    │
│ - Check file size                    │
└──────┬───────────────────────────────┘
       │
       ├─→ ❌ Invalid → Show Error
       │
       └─→ ✅ Valid → Continue
             │
             ↓
         ┌─────────────────────────┐
         │ User Clicks Upload       │
         └────┬────────────────────┘
              │
              ↓
         ┌───────────────────────────────┐
         │ submitChallengeVideo()        │
         │ - Send file to API            │
         │ - Show loading state          │
         └────┬───────────────────────────┘
              │
              ↓
         ┌──────────────────────────────┐
         │ AI Processing (2-3 sec)      │
         │ - Analyze video              │
         │ - Count reps                 │
         │ - Assess form/posture        │
         └────┬───────────────────────────┘
              │
              ↓
         ┌──────────────────────────────┐
         │ Return AI Results            │
         │ - Accuracy %                 │
         │ - Correct reps               │
         │ - Feedback                   │
         └────┬───────────────────────────┘
              │
              ↓
         ┌──────────────────────────────┐
         │ Update Challenge             │
         │ - Set status to "completed"  │
         │ - Store AI analysis          │
         │ - Update progress            │
         └────┬───────────────────────────┘
              │
              ↓
         ┌──────────────────────────────┐
         │ Display Results              │
         │ - Show AI feedback           │
         │ - Show accuracy score        │
         │ - Enable new attempts        │
         └──────────────────────────────┘
```

---

## 📁 State Management

### Component State Structure

```typescript
// MyChallengePage
selectedPlan: UserCurrentTrainingPlan | null
plans: UserCurrentTrainingPlan[]
isLoading: boolean

// TrainingPlanDetailPage
plan: TrainingPlanDetail | null
selectedDay: number
selectedChallenge: Challenge | null
isModalOpen: boolean
isUploading: boolean
isLoading: boolean

// ChallengeDetailModal
selectedFile: File | null
isUploading: boolean
```

---

## 🔌 API Integration Points

```
Frontend Components
      │
      ├─→ getCurrentTrainingPlans()
      │   └─→ GET /api/v1/training-plans/me
      │
      ├─→ getTrainingPlanDetail(planId)
      │   └─→ GET /api/v1/training-plans/{planId}
      │
      ├─→ getChallengByDay(planId, dayNum)
      │   └─→ GET /api/v1/training-plans/{planId}/days/{dayNum}
      │
      ├─→ submitChallengeVideo(planId, challengeId, video)
      │   └─→ POST /api/v1/training-plans/{planId}/challenges/{challengeId}/submit
      │
      └─→ updateChallengeStatus(planId, challengeId, status)
          └─→ PATCH /api/v1/training-plans/{planId}/challenges/{challengeId}/status
```

---

## 🎨 UI Layout Structure

```
┌─────────────────────────────────────────────────────────────┐
│                      NAVBAR                                 │
│  Logo | Home | Dashboard | My Challenge* | ... | Profile   │
└─────────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────┐
│                   MY CHALLENGE PAGE                          │
│                                                             │
│  ┌─ Back Button ─────────────────────────────────────────┐ │
│  │                                                        │ │
│  │  [Avatar] My Challenge                               │ │
│  │           Welcome back, User Name                    │ │
│  │           Track your training plans...              │ │
│  │                                                        │ │
│  │  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐   │ │
│  │  │Active Plans │ │ Completed   │ │   Streak    │   │ │
│  │  │      3      │ │      12     │ │   15 days   │   │ │
│  │  └─────────────┘ └─────────────┘ └─────────────┘   │ │
│  │                                                        │ │
│  │  Your Training Plans                                  │ │
│  │  ┌────────────────────────────────────────────────┐  │ │
│  │  │ Plan 1 | Intermediate | 40% | [Select]      │  │ │
│  │  ├────────────────────────────────────────────────┤  │ │
│  │  │ Plan 2 | Advanced      | 25% | [Select]      │  │ │
│  │  ├────────────────────────────────────────────────┤  │ │
│  │  │ Plan 3 | Beginner      | 62% | [Select]      │  │ │
│  │  └────────────────────────────────────────────────┘  │ │
│  │                                                        │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                             │
│  When Plan Selected:                                       │
│                                                             │
│  ┌────────────────────────────────────────────────────────┐│
│  │ ← Back | [Plan Name] | [Difficulty] | [User Avatar] ││
│  ├────────────────────────────────────────────────────────┤│
│  │ Progress: ████████░░░░░░░░ 40%                        ││
│  ├────────────────────────────────────────────────────────┤│
│  │ Day Tabs: [Day 1] [Day 2] [Day 3] [Day 4] ...        ││
│  ├────────────────────────────────────────────────────────┤│
│  │ Challenges for Day 1                                  ││
│  │ ┌──────────────────────────────────────────────────┐ ││
│  │ │ Challenge 1 | Sets: 3 | Reps: 10 | ✓ Completed │ ││
│  │ │ [Start] [Upload Video]                          │ ││
│  │ ├──────────────────────────────────────────────────┤ ││
│  │ │ Challenge 2 | Sets: 2 | Reps: 8  | Not Started │ ││
│  │ │ [Start] [Upload Video]                          │ ││
│  │ └──────────────────────────────────────────────────┘ ││
│  └────────────────────────────────────────────────────────┘│
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 🧩 Type System

```typescript
// Core Types
interface UserCurrentTrainingPlan {
  id: string;
  planName: string;
  description: string;
  difficulty: string;
  duration: string;
  progressPercentage: number;
  startDate: string;
  endDate: string;
  daysCompleted: number;
  totalDays: number;
  lastActivityDate: string;
}

interface TrainingPlanDetail extends UserCurrentTrainingPlan {
  status: 'active' | 'completed' | 'paused';
  userId: string;
  dayChallenges: DayChallenges[];
}

interface DayChallenges {
  dayNumber: number;
  dayName: string;
  challenges: Challenge[];
}

interface Challenge {
  id: string;
  challengeId: string;
  challengeName: string;
  sets: number;
  reps: number;
  status: 'not_started' | 'in_progress' | 'completed' | 'incorrect_form';
  description?: string;
  videoUrl?: string;
  aiAnalysis?: AIAnalysisResult;
}

interface AIAnalysisResult {
  correctReps: number;
  totalReps: number;
  accuracy: number;
  posture: string;
  feedback: string;
  suggestions?: string[];
}
```

---

## 🔐 Security Considerations

```
Authentication
├─ JWT Bearer Token in Authorization header
├─ Token included with every API request
└─ Automatic token refresh on 401

Authorization
├─ Protected route (requires login)
├─ User can only access own training plans
└─ API validates user ownership on backend

Data Security
├─ HTTPS for all communications
├─ Secure video upload with authentication
├─ Video processing on secure servers
└─ User data encryption at rest
```

---

## 📈 Performance Optimization

```
Techniques Used
├─ React.memo for component memoization
├─ useMemo for expensive computations
├─ useCallback for event handler optimization
├─ Lazy loading of images
├─ Code splitting support ready
├─ Mock data with realistic delays
└─ Efficient re-render patterns

Recommended Enhancements
├─ Virtual scrolling for large challenge lists
├─ Image lazy loading
├─ Service workers for offline support
├─ Progressive image loading
└─ Compression for video uploads
```

---

## 🧪 Testing Strategy

```
Unit Tests
├─ Component rendering tests
├─ State management tests
├─ API service tests
└─ Type validation tests

Integration Tests
├─ Navigation flow tests
├─ Video upload flow
├─ API integration tests
└─ Modal interactions

E2E Tests
├─ Complete user journey
├─ Cross-browser compatibility
├─ Responsive design verification
└─ Accessibility testing
```

---

## 🚀 Deployment Checklist

```
Pre-Deployment
☑ All tests passing
☑ No console errors
☑ Build successful
☑ Performance metrics acceptable
☑ Security review completed
☑ Documentation up-to-date

Deployment
☑ Backend API endpoints available
☑ Environment variables configured
☑ CDN configured for static assets
☑ Analytics tracking enabled
☑ Error monitoring configured
☑ Rollback plan prepared

Post-Deployment
☑ Smoke tests passing
☑ User acceptance testing
☑ Performance monitoring active
☑ Support documentation available
☑ Metrics tracking verified
```

---

**Architecture Documentation Generated:** December 4, 2025
**Status:** Complete and Ready for Implementation
